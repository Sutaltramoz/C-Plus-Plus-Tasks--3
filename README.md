# C-Plus-Plus-Tasks--3
I have implemented an interactive System Programming File I/O utility program in C++ that functions as a custom File Backup and Log Rotation utility. Instead of using hardcoded values, the program prompts the user to enter the name of the source file they want to duplicate and allows them to define a custom filename for the backup destination.
